name: KarpeSlop Audit

on:
  push:
    branches: [main]
    paths:
      - 'react-app/**'
  pull_request:
    branches: [main]
    paths:
      - 'react-app/**'

jobs:
  karpeslop:
    name: KarpeSlop Lint
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: react-app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: react-app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run KarpeSlop
        run: npx -y karpeslop@latest
